"""
Memory tests for R65.

Tests storage classes (#[zeropage], #[lowram], #[ram], #[hw], #[stack]),
implicit ROM (immutable statics), pointer types (near, far), pointer operations,
and memory access patterns.
"""
