// Test interrupt handler compilation

#[zeropage(0x20)]
static mut counter: u8 = 0;

#[entry]
#[mode(m8, x8)]
fn main() {
    counter = 0;
}

#[interrupt(nmi)]
#[mode(m8, x8, transition=auto)]
fn vblank_handler() {
    counter = counter + 1;
}
