// Test: Variable-bound parameter (non-recursive)
// Expected: Compiles successfully

#[zeropage(0x20)]
static mut TEMP: u8;

#[mode(m8, x8)]
fn add_one(value @ TEMP: u8) -> u8 {
    return value + 1;
}

#[entry]
#[mode(m8, x8)]
fn main() -> ! {
    let result @ A = add_one(5);
    loop {}
}
